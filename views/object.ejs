<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>cartas</title>
    <style>
body {
    font-family: montserrat;
    background-color: #eef2f5;
    margin: 0;
    padding: 20px;
    background-image: linear-gradient(to bottom left, #b69780, #4b2e15); 
    color: #333;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
}

h1 {
    font-size: 3em;
    color: #ffffff;
    text-align: center;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 3px;
    border-bottom: 3px solid #5c4033; 
    padding-bottom: 10px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3); 
}

img {
    max-width: 100%;
    max-height: 400px;
    border-radius: 8px;
    box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.3); 
    margin-bottom: 20px;
}

p {
    font-size: 1.2em;
    color: #000000; 
    margin-bottom: 10px;
}

h2 {
    font-size: 2em;
    color: #ffffff;
    text-align: center;
    margin-top: 40px;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 2px;
    border-bottom: 2px solid #5c4033; 
    padding-bottom: 8px;
}

.grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    width: 100%;
    max-width: 1200px;
    margin-top: 20px;
}

.card {
    background-color: #ffffff;
    border-radius: 10px;
    box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.3); 
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    justify-content: center;
    align-items: center;
}

.card img {
    max-width: 100%;
    height: auto;
    transition: opacity 0.3s ease;
}

.card:hover {
    transform: scale(1.05);
    box-shadow: 0px 12px 20px rgba(0, 0, 0, 0.4); 
}

a {
    text-decoration: none;
    color: #ffffff;
    font-weight: bold;
    padding: 12px 25px;
    border: 2px solid #8b5e3c; 
    border-radius: 6px;
    background-color: #8b5e3c; 
    transition: background-color 0.3s, color 0.3s, transform 0.3s;
    text-align: center;
    font-size: 1.1em;
    margin-top: 30px;
}

a:hover {
    background-color: #4b2e15; 
    border-color: #4b2e15;
    color: #ffffff;
    transform: translateY(-2px);
}
    </style>
</head>
<body>

    <h1><%= title %></h1>
    
    <img src="<%= imageUrl %>" alt="<%= title %>">
    <p><strong>Cultura:</strong> <%= culture %></p>
    <p><strong>Dinastia:</strong> <%= dynasty %></p>
    <p><strong>Fecha:</strong> <%= date %></p>

    <% if (additionalImages.length > 0) { %>
    <h2>imagenes extras</h2>
    <div class="grid">
        <% additionalImages.forEach(img => { %>
            <div class="card">
                <img src="<%= img %>" alt="imagen extra de <%= title %>">
            </div>
    <% }) %>
    </div>
    <% } %>
    <a href="javascript:history.back()">Volver busqueda</a>
    <a href="/">Volver inicio</a>
</body>
</html>